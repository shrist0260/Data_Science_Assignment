# Convert a list into an iterator and print elements using next( ).

# Create a list
numbers = [10, 20, 30, 40, 50]

# Convert list into iterator
iterator = iter(numbers)

# Print elements using next()
print(next(iterator))
print(next(iterator))
print(next(iterator))
print(next(iterator))
print(next(iterator))
